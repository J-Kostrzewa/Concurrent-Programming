<Window x:Class="TP.ConcurrentProgramming.PresentationView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TP.ConcurrentProgramming.Presentation.ViewModel;assembly=TP.ConcurrentProgramming.PresentationViewModel"
        mc:Ignorable="d"
        Title="Presentation layer targeting reactive behaviour" 
        SizeChanged="Window_SizeChanged"
        MinWidth="450" MinHeight="500">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Przyciski sterujące -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10" VerticalAlignment="Center" HorizontalAlignment="Center">
            <!-- Przycisk zmniejszający liczbę kul -->
            <Button Content="-" Command="{Binding DecreaseBallCountCommand}" Margin="5" Width="30" />

            <!-- Pole wyświetlające liczbę kul -->
            <TextBlock Text="{Binding BallCount}" VerticalAlignment="Center" FontSize="16" Margin="5" Width="50" TextAlignment="Center" />

            <!-- Przycisk zwiększający liczbę kul -->
            <Button Content="+" Command="{Binding IncreaseBallCountCommand}" Margin="5" Width="30" />

            <!-- Przycisk start -->
            <Button Content="Start" Command="{Binding StartCommand}" Margin="5" Width="75" />
        </StackPanel>

        <!-- Obszar wyświetlania kul -->
        <Border Grid.Row="1" BorderThickness="4" BorderBrush="Green" Margin="10">
            <Viewbox Stretch="Uniform">
                <Canvas Width="400" Height="420">
                    <ItemsControl ItemsSource="{Binding Balls}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Ellipse Width="{Binding Diameter}" Height="{Binding Diameter}" Fill="Blue" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Canvas.Top" Value="{Binding Top}" />
                                <Setter Property="Canvas.Left" Value="{Binding Left}" />
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                    </ItemsControl>
                </Canvas>
            </Viewbox>
        </Border>
    </Grid>
</Window>